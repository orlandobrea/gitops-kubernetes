apiVersion: v1
kind: Service
metadata:
  name: dashboard-migration-sips-be-svc
  annotations:
    mesh.traefik.io/traffic-type: "http"
    mesh.traefik.io/retry-attemps: "2"
spec:
  type: ClusterIP
  ports: 
    - name: web
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: dashboard-migration-sips-be

