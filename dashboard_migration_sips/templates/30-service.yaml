apiVersion: v1
kind: Service
metadata:
  name: dashboard-migration-sips-fe-svc
  annotations:
    mesh.traefik.io/traffic-type: "http"
spec:
  type: ClusterIP
  ports: 
    - name: web
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: dashboard-migration-sips-fe

